{
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["@tauri-apps/*"],
            "message": "@scriptz/core must stay platform-neutral. Tauri-specific glue belongs in apps/desktop/src/lib/platform.ts (the host adapter). If you need a native capability here, add a method to PlatformAdapter in lib/platform.ts and call it via getPlatformAdapter()."
          }
        ]
      }
    ]
  }
}
