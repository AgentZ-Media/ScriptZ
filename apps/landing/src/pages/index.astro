---
/**
 * Deutsche Landing - `/` (Default-Sprache).
 * Englische Variante liegt unter `/en` und teilt sich Markup und CSS
 * via `LandingPage.astro`.
 */
import Base from "../layouts/Base.astro";
import LandingPage from "../components/LandingPage.astro";
import { t } from "../i18n";
---
<Base
  lang="de"
  title={t("de", "meta.title")}
  description={t("de", "meta.description")}
>
  <LandingPage lang="de" />
</Base>
