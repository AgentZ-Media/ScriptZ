---
/**
 * `/blog/<slug>` - detail page of a blog post (German).
 *
 * `getStaticPaths` lists all DE posts. EN posts run via the
 * counterpart at `/en/blog/<slug>`. Both slugs are identical so the
 * language toggle can form a clean sister link.
 */
import Base from "../../layouts/Base.astro";
import AppShell from "../../components/AppShell.astro";
import BlogPost from "../../components/blog/BlogPost.astro";
import {
  getPosts,
  hasTranslation,
  parseEntryId,
  postPath,
  blogIndexPath,
} from "../../lib/blog";
import { type Lang } from "../../i18n";

export async function getStaticPaths() {
  const posts = await getPosts("de");
  return posts.map((entry) => {
    const parsed = parseEntryId(entry.id)!;
    return {
      params: { slug: parsed.slug },
      props: { entry },
    };
  });
}

const lang: Lang = "de";
const { entry } = Astro.props;
const slug = parseEntryId(entry.id)!.slug;

const hasOther = await hasTranslation(slug, "en");
const switchHref = hasOther ? postPath(slug, "en") : blogIndexPath("en");
---

<Base
  title={`${entry.data.title} - ScriptZ Blog`}
  description={entry.data.description}
  lang={lang}
>
  <AppShell
    lang={lang}
    activeTab="blog"
    toolbarTitleOverride={entry.data.title}
    switchLangHrefOverride={switchHref}
  >
    <BlogPost entry={entry} lang={lang} />
  </AppShell>
</Base>
