# Convex deployment URL - filled by `npx convex dev` into .env.local.
# Copy this file to .env.local (which is gitignored) or let the Convex
# CLI write it for you. The frontend reads it via import.meta.env.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
